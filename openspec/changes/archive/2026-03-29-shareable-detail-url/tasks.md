# Tasks: Shareable Detail URL

## Tasks

- [x] 1. Add `toSlug` and `getAlgorithmBySlug` to `useAlgorithms.js`
- [x] 2. Add hash read/write logic to `App.vue` (on load, on open, on close)
- [x] 3. Add "Copy Link" button with "Copied!" feedback to `DetailModal.vue`
- [x] 4. Verify deep link works in dev server (manual test)
