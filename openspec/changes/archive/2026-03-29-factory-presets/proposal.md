# Proposal: Factory Presets & Preset Search

## Problem
Users cannot see which factory presets belong to each algorithm, and cannot search by preset name.

## Solution
1. Extract factory preset names from the H90 manual for all algorithms
2. Display presets as pill tags in the detail modal
3. Add preset name to the search filter so users can find algorithms by preset name
